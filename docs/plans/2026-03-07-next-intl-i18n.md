# Next-intl i18n (cs/de) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add `next-intl` internationalization with locale-prefixed routing for Czech and German in a Next.js 16 App Router project.

**Architecture:** Use `next-intl` routing + request config, route through `app/[locale]`, and handle locale redirects with `proxy.ts`. Translation messages are JSON files loaded per locale.

**Tech Stack:** Next.js 16, React 19, TypeScript, next-intl, Bun

---

### Task 1: Install and wire next-intl

**Files:**
- Modify: `/Users/baudysdev/Coding/www/zamek-lazen/package.json`
- Modify: `/Users/baudysdev/Coding/www/zamek-lazen/next.config.ts`

**Step 1: Install dependency**

Run: `bun add next-intl`
Expected: `next-intl` added to dependencies.

**Step 2: Configure Next plugin**

Edit `next.config.ts` to wrap config with `createNextIntlPlugin("./i18n/request.ts")`.

**Step 3: Verify install/config**

Run: `bun run lint`
Expected: No config/type errors from plugin setup.

### Task 2: Add i18n routing and proxy

**Files:**
- Create: `/Users/baudysdev/Coding/www/zamek-lazen/i18n/routing.ts`
- Create: `/Users/baudysdev/Coding/www/zamek-lazen/i18n/request.ts`
- Create: `/Users/baudysdev/Coding/www/zamek-lazen/proxy.ts`

**Step 1: Define locales**

Add routing with locales `["cs", "de"]`, default locale `cs`, and locale prefix mode `always`.

**Step 2: Add request config**

Load translation JSON based on request locale and fall back to default locale when invalid.

**Step 3: Add Next.js 16 proxy**

Create `proxy.ts` using `createMiddleware(routing)` and matcher excluding API/static assets.

### Task 3: Move app to locale segment and add messages

**Files:**
- Create: `/Users/baudysdev/Coding/www/zamek-lazen/app/[locale]/layout.tsx`
- Create: `/Users/baudysdev/Coding/www/zamek-lazen/app/[locale]/page.tsx`
- Modify: `/Users/baudysdev/Coding/www/zamek-lazen/app/layout.tsx`
- Delete: `/Users/baudysdev/Coding/www/zamek-lazen/app/page.tsx`
- Create: `/Users/baudysdev/Coding/www/zamek-lazen/messages/cs.json`
- Create: `/Users/baudysdev/Coding/www/zamek-lazen/messages/de.json`

**Step 1: Locale layout**

Validate locale and wrap children in `NextIntlClientProvider`.

**Step 2: Localized page**

Render content with `useTranslations` keys from messages files.

**Step 3: Root layout cleanup**

Keep global shell (fonts, CSS) and host localized subtree via `children`.

### Task 4: Validate build and quality

**Files:**
- Modify (if needed): `/Users/baudysdev/Coding/www/zamek-lazen/README.md`

**Step 1: Lint**

Run: `bun run lint`
Expected: PASS.

**Step 2: Build**

Run: `bun run build`
Expected: PASS with localized routes generated.

**Step 3: Document usage**

If needed, update README to note locale URLs (`/cs`, `/de`).

Plan complete and saved to `docs/plans/2026-03-07-next-intl-i18n.md`.
